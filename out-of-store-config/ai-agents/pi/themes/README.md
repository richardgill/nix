# Pi themes

`tokyo-night.json` is a local custom copy based on @ifi/oh-pi-themes' Tokyo Night values:
https://github.com/ifiokjr/oh-pi/blob/main/packages/themes/themes/tokyo-night.json

It keeps local HTML export colors in the `export` block.
