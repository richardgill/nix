#!/usr/bin/env bash
set -euo pipefail

command="${1:-}"
if [[ -z "$command" ]]; then
  echo "Usage: launch-terminal-app <command> [title]" >&2
  exit 1
fi

title="${2:-$command}"

uwsm app -- "$HOME/Scripts/nixos/launch-ghostty" --title="$title" --confirm-close-surface=false -e "$command"
