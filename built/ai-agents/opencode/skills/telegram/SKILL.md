---
name: telegram
description: Send Richard a Telegram message via the local telegram CLI.
---

Use `telegram` to send Telegram messages.

Tracked sends are the default and route replies back when the current workflow provides session context.

Examples:

```bash
telegram "Need your input on this change"
telegram "Build passed"
```
