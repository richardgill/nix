#!/usr/bin/env bash
set -euo pipefail

APP_NAME="$1"

if [[ -z "$APP_NAME" ]]; then
  echo "Usage: checkWebappOpen <app-name>"
  exit 1
fi

hyprctl clients -j | jq -e ".[] | select(.class | test(\"chrome-app.$APP_NAME\"))"
