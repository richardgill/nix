#!/usr/bin/env bash

# Exit on any error
set -e

# Get the directory of this script
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

"$SCRIPT_DIR/auto-commit"
git push
"$SCRIPT_DIR/auto-pr"

